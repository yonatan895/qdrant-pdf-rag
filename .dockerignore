# Keep build contexts small; wheels and BM25 weights come in via named
# build contexts (see Makefile build-images), never from the repo tree.
.git
.venv
venv
__pycache__
*.py[cod]
.pytest_cache
.mypy_cache
.ruff_cache
output
bundles
dist
build
tests
docs
deploy
overlays
charts
oc-mirror
.github
.agents
vendor
NOTICE.qdrant-skills
LICENSE.qdrant-skills
# CI-only mock server: must never ship inside a prod image (runs from a
# ConfigMap in the rehearsal instead).
scripts/mock_vllm.py
