# Air-gap tasks — issue #402 increment B4.
#
# Thin wrappers only: every stage keeps its validation, sequencing, retry and
# structured-error logic in scripts/airgap/*.sh (pipeline.sh owns the
# validate -> load -> deploy -> ingest -> smoke order; Task never
# reconstructs it as a dependency graph). The supported `sh
# scripts/airgap/<stage>.sh` entry points stay for troubleshooting.
#
# Operator settings flow exactly like Make: every documented operator key is
# bridged per task, so `task airgap:deploy INTERNAL_REGISTRY=x` (CLI form)
# and `INTERNAL_REGISTRY=x task airgap:deploy` (env form) both reach
# common.sh, which gives explicit values precedence over airgap.env while
# empty stays unset. There is deliberately no root dotenv and no default for
# any operator key here. The bridge set of every task must equal
# OPERATOR_ENV_KEYS in scripts/airgap/common.sh (pinned by contract test).
# Included with `dir: .` from the root Taskfile: always runs at repo root.
version: '3'

tasks:
  pack:
    desc: Build the signed sneakernet bundle (connected host only)
    summary: |
      `sh scripts/airgap/pack.sh`, mirroring `make airgap-pack`. Needs
      IMAGE_SHA (must equal git HEAD) and the signing key in the caller
      environment. Connected image factory only; the gap never builds.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/pack.sh

  load:
    desc: Load bundle images into the cluster registry
    summary: |
      `sh scripts/airgap/load.sh`, mirroring `make airgap-load`. Follows
      ARTDIR discovery for the unpacked bundle.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/load.sh

  deploy:
    desc: Deploy the application over the loaded images
    summary: |
      `sh scripts/airgap/deploy.sh`, mirroring `make airgap-deploy`.
      Renders with explicit caller values winning over the env file; see the
      module header for the precedence rule.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/deploy.sh

  ingest:
    desc: Ingest the corpus through the deployed pipeline
    summary: |
      `sh scripts/airgap/ingest.sh`, mirroring `make airgap-ingest`.
      Needs CORPUS_PVC pointing at the prepared claim.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/ingest.sh

  smoke:
    desc: Smoke the deployed search path with a literal query
    summary: |
      `sh scripts/airgap/smoke.sh`, mirroring `make airgap-smoke`.
      QUERY arrives as the script's quoted value; run ingest first.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/smoke.sh

  validate:
    desc: Validate bundle, workspace and configuration without mutating
    summary: |
      `sh scripts/airgap/validate.sh`, mirroring `make airgap-validate`.
      With AIRGAP_DRYRUN=1 it renders against stand-ins; without it, it
      checks the real workspace. Prints the next step on success.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/validate.sh

  pipeline:
    desc: 'Run the full air-gap pipeline in stage order (owner: pipeline.sh)'
    summary: |
      `sh scripts/airgap/pipeline.sh`, mirroring `make airgap-pipeline`.
      The script owns stage order and skipping; this task only forwards the
      operator environment. Not reconstructed as a Task dependency graph.
    env:
      AGENT_ROUTE: '{{.AGENT_ROUTE}}'
      AIRGAP_APP_REGISTRY: '{{.AIRGAP_APP_REGISTRY}}'
      AIRGAP_BUNDLE_DIR: '{{.AIRGAP_BUNDLE_DIR}}'
      AIRGAP_DRYRUN: '{{.AIRGAP_DRYRUN}}'
      AIRGAP_WORKSPACE: '{{.AIRGAP_WORKSPACE}}'
      CONTEXTUAL_EMBED_ENABLED: '{{.CONTEXTUAL_EMBED_ENABLED}}'
      CONTEXT_LLM_BASE_URL: '{{.CONTEXT_LLM_BASE_URL}}'
      CONTEXT_LLM_MODEL: '{{.CONTEXT_LLM_MODEL}}'
      CORPUS_PVC: '{{.CORPUS_PVC}}'
      DENSE_DIM: '{{.DENSE_DIM}}'
      EMBED_BASE_URL: '{{.EMBED_BASE_URL}}'
      EMBED_MODE: '{{.EMBED_MODE}}'
      EMBED_MODEL: '{{.EMBED_MODEL}}'
      EMBED_MODEL_REVISION: '{{.EMBED_MODEL_REVISION}}'
      GATEWAY_API_KEY_SECRET: '{{.GATEWAY_API_KEY_SECRET}}'
      GATEWAY_CA_CONFIGMAP: '{{.GATEWAY_CA_CONFIGMAP}}'
      GHCR_OWNER: '{{.GHCR_OWNER}}'
      IMAGE_SHA: '{{.IMAGE_SHA}}'
      INGEST_ALIAS_PUBLISH: '{{.INGEST_ALIAS_PUBLISH}}'
      INGEST_EXTRA_PATCH: '{{.INGEST_EXTRA_PATCH}}'
      INGEST_REINGEST: '{{.INGEST_REINGEST}}'
      INGEST_RETIRE_DOCS: '{{.INGEST_RETIRE_DOCS}}'
      INGEST_TIMEOUT: '{{.INGEST_TIMEOUT}}'
      INGEST_WORKERS: '{{.INGEST_WORKERS}}'
      INGEST_WORK_SIZE: '{{.INGEST_WORK_SIZE}}'
      INSECURE_REGISTRY: '{{.INSECURE_REGISTRY}}'
      INTERNAL_REGISTRY: '{{.INTERNAL_REGISTRY}}'
      JAEGER_QUERY_URL: '{{.JAEGER_QUERY_URL}}'
      KC: '{{.KC}}'
      LLM_BASE_URL: '{{.LLM_BASE_URL}}'
      LLM_MODEL_REASONING: '{{.LLM_MODEL_REASONING}}'
      METRICS_ENABLED: '{{.METRICS_ENABLED}}'
      NAMESPACE: '{{.NAMESPACE}}'
      OPENSHIFT_NAMESPACE: '{{.OPENSHIFT_NAMESPACE}}'
      OTEL_DEPLOYMENT_ENVIRONMENT: '{{.OTEL_DEPLOYMENT_ENVIRONMENT}}'
      OTEL_EXPORTER_OTLP_ENDPOINT: '{{.OTEL_EXPORTER_OTLP_ENDPOINT}}'
      OTEL_SERVICE_NAME: '{{.OTEL_SERVICE_NAME}}'
      PULL_SECRET: '{{.PULL_SECRET}}'
      QDRANT_EXTRA_VALUES: '{{.QDRANT_EXTRA_VALUES}}'
      QDRANT_IMAGE: '{{.QDRANT_IMAGE}}'
      QDRANT_RELEASE: '{{.QDRANT_RELEASE}}'
      QDRANT_STORAGE_SIZE: '{{.QDRANT_STORAGE_SIZE}}'
      QDRANT_TAG: '{{.QDRANT_TAG}}'
      QUERY: '{{.QUERY}}'
      REGISTRY_INTERNAL: '{{.REGISTRY_INTERNAL}}'
      RERANK_BASE_URL: '{{.RERANK_BASE_URL}}'
      RERANK_ENABLED: '{{.RERANK_ENABLED}}'
      RERANK_ENDPOINT_ORDER: '{{.RERANK_ENDPOINT_ORDER}}'
      RERANK_MODEL: '{{.RERANK_MODEL}}'
      SKOPEO_ARGS: '{{.SKOPEO_ARGS}}'
      SNAPSHOT_STORAGE_CLASS: '{{.SNAPSHOT_STORAGE_CLASS}}'
      SNEAKERNET_KEY_TRUSTED: '{{.SNEAKERNET_KEY_TRUSTED}}'
      SNEAKERNET_SIGNING_KEY: '{{.SNEAKERNET_SIGNING_KEY}}'
      SNEAKERNET_TRUSTED_PUB: '{{.SNEAKERNET_TRUSTED_PUB}}'
      STORAGE_CLASS: '{{.STORAGE_CLASS}}'
      VLLM_BASE_URL: '{{.VLLM_BASE_URL}}'
    cmds:
      - sh scripts/airgap/pipeline.sh

  dryrun:
    desc: Execute the real hermetic pipeline validation (not a preview)
    summary: |
      The fixed dry-run stand-ins plus `sh scripts/airgap/pipeline.sh
      --dry-run`, mirroring `make airgap-dryrun`. This EXECUTES the
      validation/rendering path; `task --dry` (command preview) is not
      acceptance evidence. Stand-ins win over caller and file values, same
      as the Make recipe environment.
    cmds:
      - AIRGAP_DRYRUN=1 IMAGE_SHA="$(git rev-parse HEAD 2>/dev/null || echo "0000000000000000000000000000000000000000")" INTERNAL_REGISTRY=registry.example.internal/mainframe-rag NAMESPACE=mainframe-rag STORAGE_CLASS=gp3-csi EMBED_MODEL=ibm-granite/granite-embedding-278m-multilingual DENSE_DIM=768 EMBED_MODEL_REVISION=dryrun-rev-1 VLLM_BASE_URL=http://vllm.inference.svc.cluster.local:8000/v1 CORPUS_PVC=corpus-pvc sh scripts/airgap/pipeline.sh --dry-run
